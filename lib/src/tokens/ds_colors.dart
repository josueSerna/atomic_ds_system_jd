import 'package:flutter/material.dart';

class DsColors {
  DsColors._();

  /// Azul principal de Pokémon TCG.
  static const Color blue = Color(0xFF2A52BE);

  /// Amarillo principal de Pokémon TCG.
  static const Color yellow = Color(0xFFFFCB05);

  /// Rojo principal de Pokémon TCG.
  static const Color red = Color.fromARGB(255, 175, 0, 0);

  /// Blanco para fondos y contenedores.
  static const Color white = Color(0xFFFFFFFF);

  /// Negro para textos y elementos de contraste.
  static const Color black = Color(0xFF000000);

  /// Gris claro para elementos secundarios.
  static const Color greyLight = Color(0xFFF5F5F5);

  /// Gris medio para bordes y divisores.
  static const Color greyMedium = Color(0xFFE0E0E0);

  /// Gris oscuro para textos secundarios.
  static const Color greyDark = Color(0xFF757575);

  // Colores de tipos Pokémon oficiales
  /// Verde - Tipo Grass/Planta.
  static const Color grass = Color(0xFF78C850);

  /// Naranja - Tipo Fire/Fuego.
  static const Color fire = Color(0xFFF08030);

  /// Azul - Tipo Water/Agua.
  static const Color water = Color(0xFF6890F0);

  /// Amarillo - Tipo Electric/Eléctrico.
  static const Color electric = Color(0xFFF8D030);

  /// Rosa - Tipo Psychic/Psíquico.
  static const Color psychic = Color(0xFFF85888);

  /// Beige - Tipo Normal.
  static const Color normal = Color(0xFFA8A878);

  /// Rojo oscuro - Tipo Fighting/Lucha.
  static const Color fighting = Color(0xFFC03028);

  /// Morado - Tipo Poison/Veneno.
  static const Color poison = Color(0xFFA040A0);

  /// Marrón claro - Tipo Ground/Tierra.
  static const Color ground = Color(0xFFE0C068);

  /// Lila - Tipo Flying/Volador.
  static const Color flying = Color(0xFFA890F0);

  /// Verde lima - Tipo Bug/Bicho.
  static const Color bug = Color(0xFFA8B820);

  /// Marrón - Tipo Rock/Roca.
  static const Color rock = Color(0xFFB8A038);

  /// Púrpura oscuro - Tipo Ghost/Fantasma.
  static const Color ghost = Color(0xFF705898);

  /// Azul-púrpura - Tipo Dragon/Dragón.
  static const Color dragon = Color(0xFF7038F8);

  /// Marrón oscuro - Tipo Dark/Siniestro.
  static const Color dark = Color(0xFF705848);

  /// Gris plateado - Tipo Steel/Acero.
  static const Color steel = Color(0xFFB8B8D0);

  /// Rosa claro - Tipo Fairy/Hada.
  static const Color fairy = Color(0xFFEE99AC);

  /// Celeste - Tipo Ice/Hielo.
  static const Color ice = Color(0xFF98D8D8);

  // ── Colores de UI — Light ──

  /// Fondo del scaffold en modo claro.
  static const Color lightBackground = Color(0xFFFFFFFF);

  /// Superficie elevada en modo claro (tarjetas, sheets).
  static const Color lightSurface = Color(0xFFF7F8FA);

  /// Fondo de tarjetas en modo claro.
  static const Color cardBackground = Color(0xFFF0F2F5);

  /// Color de sombra.
  static const Color shadow = Color(0x1A000000);

  /// Texto principal en modo claro (títulos).
  static const Color lightTextPrimary = Color(0xFF1A1A2E);

  /// Texto secundario en modo claro (body, labels).
  static const Color lightTextSecondary = Color(0xFF5C5C70);

  /// Divisores y bordes en modo claro.
  static const Color lightDivider = Color(0xFFE0E2E8);

  // ── Colores de UI — Dark ──

  /// Fondo del scaffold en modo oscuro (azul noche tenue).
  static const Color darkBackground = Color(0xFF0F1626);

  /// Superficie elevada en modo oscuro.
  static const Color darkSurface = Color(0xFF1A2238);

  /// Fondo de tarjetas en modo oscuro.
  static const Color darkCard = Color(0xFF243052);

  /// Texto principal en modo oscuro (títulos blancos).
  static const Color darkTextPrimary = Color(0xFFFFFFFF);

  /// Texto secundario en modo oscuro.
  static const Color darkTextSecondary = Color(0xFFA8B0C8);

  /// Divisores y bordes en modo oscuro.
  static const Color darkDivider = Color(0xFF2E3A56);
}
